name: Ausbildung Job Scraper

on:
  schedule:
    # Two complete collections per day at 06:00 and 18:00 UTC.\n    - cron: '0 6,18 * * *'
  workflow_dispatch:

concurrency:
  group: ausbildung-twice-daily-scraper
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  scrape_jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          check-latest: false

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml

      - name: Validate Python syntax
        run: python -m py_compile ausbildung_scraper.py daily_runner.py

      - name: Run twice-daily mass scraper
        env:
          GOOGLE_SHEET_WEBHOOK_URL: ${{ secrets.GOOGLE_SHEET_WEBHOOK_URL }}
        run: python -u daily_runner.py
